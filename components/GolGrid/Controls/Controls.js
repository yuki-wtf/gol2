import React from "react";
import styled from "styled-components";

const StyledControls = styled.div``;

const Controls = () => {
  return <StyledControls>Controls</StyledControls>;
};

export default Controls;
